"use client";
import {useEffect,useState} from "react";
import {useRouter} from "next/navigation";
const API="http://15.252.146.207:5000/api/v1";
export default function Courses(){const[r,setR]=useState([]),[q,setQ]=useState(""),[loading,setLoading]=useState(true),router=useRouter();useEffect(()=>{fetch(API+"/courses").then(x=>x.json()).then(x=>setR(Array.isArray(x?.data)?x.data:[])).finally(()=>setLoading(false))},[]);const list=r.filter(x=>String(x.name).toLowerCase().includes(q.toLowerCase()));return <main className="min-h-screen bg-[#f4f3ef] px-5 py-28"><div className="max-w-6xl mx-auto"><div className="flex justify-between items-end gap-4 mb-8"><div><p className="text-xs font-bold tracking-[.2em] text-zinc-500">DNS ACADEMY</p><h1 className="text-4xl font-black mt-2">Courses</h1></div><span className="text-sm text-zinc-500">{list.length} courses</span></div><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Search courses..." className="w-full max-w-xl rounded-2xl border border-zinc-300 bg-white px-5 py-4 outline-none mb-8"/>{loading?<p>Loading...</p>:<div className="grid sm:grid-cols-2 lg:grid-cols-3 gap-5">{list.map(c=><button key={c.id} onClick={()=>router.push("/courses/"+c.id)} className="text-left rounded-3xl bg-white border border-zinc-200 p-5 shadow-sm hover:shadow-xl hover:-translate-y-1 transition"><div className="h-36 rounded-2xl bg-zinc-100 overflow-hidden">{c.imageUrl&&<img src={c.imageUrl} alt="" className="w-full h-full object-cover"/>}</div><h2 className="font-black text-xl mt-4">{c.name}</h2><p className="text-sm text-zinc-500 mt-1">{c.subject?.name||"DNS Academy"}</p><div className="mt-5 flex justify-between items-center"><b>₹{Number(c.price||0).toLocaleString("en-IN")}</b><span className="px-4 py-2 rounded-xl bg-black text-white text-sm">View →</span></div></button>)}</div>}</div></main>}
