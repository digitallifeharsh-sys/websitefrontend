"use client";
import {useEffect,useState} from "react";import {useRouter} from "next/navigation";
export default function Profile(){const[user,setUser]=useState(null),router=useRouter();useEffect(()=>{const raw=localStorage.getItem("authUser");if(!raw){router.push("/auth/number");return;}try{setUser(JSON.parse(raw))}catch{router.push("/auth/number")}},[router]);const logout=()=>{localStorage.removeItem("accessToken");localStorage.removeItem("refreshToken");localStorage.removeItem("authUser");router.push("/auth/number")};if(!user)return <main className="min-h-screen p-10">Loading...</main>;const name=user.name||user.fullName||user.username||"User";return <main className="min-h-screen bg-[#f4f3ef] px-5 py-28"><div className="max-w-xl mx-auto"><div className="rounded-3xl bg-black text-white p-7"><div className="w-16 h-16 rounded-2xl bg-white text-black flex items-center justify-center text-2xl font-black">{name[0]?.toUpperCase()}</div><h1 className="text-3xl font-black mt-5">{name}</h1><p className="text-zinc-400 mt-1">{user.phone||user.mobile||""}</p></div><div className="grid grid-cols-2 gap-3 mt-4"><button onClick={()=>router.push("/my-courses")} className="rounded-2xl bg-white border border-zinc-200 p-5 text-left font-bold">My Learning →</button><button onClick={logout} className="rounded-2xl bg-white border border-zinc-200 p-5 text-left font-bold text-red-600">Logout →</button></div></div></main>}
