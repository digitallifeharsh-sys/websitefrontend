"use client";
import {useEffect,useState} from "react";import {useRouter} from "next/navigation";
const API="http://15.252.146.207:5000/api/v1";
export default function MyCourses(){const[items,setItems]=useState([]),[loading,setLoading]=useState(true),router=useRouter();useEffect(()=>{const t=localStorage.getItem("accessToken");if(!t){router.push("/auth/number");return;}fetch(API+"/my-courses",{headers:{Authorization:"Bearer "+t}}).then(r=>r.json()).then(x=>setItems(x.data||[])).finally(()=>setLoading(false))},[router]);return <main className="min-h-screen bg-[#f4f3ef] px-5 py-28"><div className="max-w-5xl mx-auto"><p className="text-xs font-bold tracking-[.2em] text-zinc-500">DNS ACADEMY</p><h1 className="text-4xl font-black mt-2">My Courses</h1>{loading?<p className="mt-8">Loading...</p>:<div className="grid sm:grid-cols-2 lg:grid-cols-3 gap-5 mt-8">{items.map(c=><button key={c.id} onClick={()=>router.push("/my-courses/"+c.id)} className="text-left rounded-3xl bg-white border border-zinc-200 p-5 shadow-sm hover:-translate-y-1 transition"><h2 className="font-black text-lg">{c.name}</h2><p className="text-sm text-zinc-500 mt-1">{c.subject||"DNS Academy"}</p><span className="inline-block mt-6 rounded-xl bg-black text-white px-4 py-2 text-sm">Continue →</span></button>)}</div>}</div></main>}
