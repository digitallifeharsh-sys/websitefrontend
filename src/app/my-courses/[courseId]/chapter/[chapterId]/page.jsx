"use client";
import {useEffect,useState} from "react";import {useParams,useRouter} from "next/navigation";
const API="http://15.252.146.207:5000/api/v1";
export default function Chapter(){const{courseId,chapterId}=useParams(),router=useRouter(),[d,setD]=useState(null),[err,setErr]=useState("");useEffect(()=>{const t=localStorage.getItem("accessToken");if(!t){router.push("/auth/number");return;}fetch(API+"/my-courses/"+courseId+"/chapters/"+chapterId,{headers:{Authorization:"Bearer "+t}}).then(r=>r.json()).then(x=>x.success?setD(x.data):setErr(x.message||"Unable to load chapter"))},[courseId,chapterId,router]);if(err)return <main className="min-h-screen p-10">{err}</main>;if(!d)return <main className="min-h-screen p-10">Loading...</main>;return <main className="min-h-screen bg-[#f4f3ef] px-5 py-28"><div className="max-w-5xl mx-auto"><h1 className="text-3xl font-black">{d.chapter?.name}</h1><div className="mt-7 space-y-6">{(d.videos||[]).map(v=><Video key={v.id} video={v}/>)}</div></div></main>}
function Video({video}){return <div className="rounded-3xl bg-white border border-zinc-200 p-4"><h2 className="font-black mb-3">{video.title}</h2><div className="aspect-video rounded-2xl overflow-hidden bg-black"><video controls controlsList="nodownload noplaybackrate" disablePictureInPicture disableRemotePlayback className="w-full h-full" src={video.videoUrl}/></div></div>}
